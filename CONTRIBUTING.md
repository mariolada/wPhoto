# Feedback and contributions

This repository accepts bug reports and feature requests through GitHub Issues.
It does not accept public code contributions or pull requests because the
development repository is private.

When reporting a problem, include the wPhoto version, Windows version, the media
type involved and a short sequence of steps. Remove personal filenames, device
identifiers and private folder paths before posting.
